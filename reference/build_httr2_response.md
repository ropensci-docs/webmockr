# Build a httr2 response (`httr2_response`)

Build a httr2 response (`httr2_response`)

## Usage

``` r
build_httr2_response(req, resp)
```

## Arguments

- req:

  a request

- resp:

  a response

## Value

an httr2 response (`httr2_response`)
